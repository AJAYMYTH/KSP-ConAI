# KSP Crime Intelligence Copilot — Environment Variables Template

# Base API Integration URL (points to Zoho Catalyst serverless endpoint or Local Advanced I/O emulation)
PUBLIC_API_URL=http://localhost:3000/server

# WebSocket URL (only if real-time synchronization is needed in future)
PUBLIC_WS_URL=ws://localhost:3000/server/ws

# Mock Data Mode: set to true to bypass backend connections and use high-fidelity simulated datasets
PUBLIC_MOCK_MODE=false
